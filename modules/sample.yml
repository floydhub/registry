name: Shuffle File Rows
description: >
    Randomly shuffles the order of rows in a text file
id: EgtNveyrRyJZ8iGUBPfkMP
version: 0.1
repo: https://github.com/floydhub/textutil-shuffle-file.git
branch: master
owner: floydhub@gmail.com
command: "python shuffle.py -i {{file}} -o {{shuff_file}} -header {{has_header}}"
params:
    - name: has_header
      type: bool
      default: False
      description: Does file have header row?
      min: 
      max: 
      enum: 
inputs:
    - name: file
      type: text
      description: Input text file to shuffle
outputs:
    - name: shuff_file
      type: text
      description: Shuffled output text file
default_container: docker:python
predecessor: 
language: Python
tags: [text, util]